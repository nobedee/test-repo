name: It Worked
run-name: ${{ github.actor }} ran
on: [create]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.repository }}
      - run: ls -al      
      - name: Clone
        uses: actions/checkout@v3
      - run: cat server.js
      - run: echo the ${{ github.repository }} clone
      - run: ls
      - name: List files 
        run: |
          echo files already listed
          echo it worked
          echo job complete
